#include "sdk.h"




//
//impl

//system::CreateTimer(FunctionPointer<void(void*)> fn, void * data, Float64 interval, Priority priority, Reflex::Allocator & allocator)

// CFAbsoluteTime now = CFAbsoluteTimeGetCurrent();

// CFRunLoopTimerContext context;
// std::memset(&context, 0, sizeof(context));
// context.info = ...;

// CFRunLoopTimerRef timerRef = CFRunLoopTimerCreate(kCFAllocatorDefault, now + interval, interval, 0, 0, RunLoopTimerCallBack, &context);

// // Add it to the loop.
// CFRunLoopAddTimer(CFRunLoopGetCurrent(), timerRef, kCFRunLoopDefaultMode);

//CreateThread(Start::Call()

//
// System::Thread::StopTimer()
// {
	// CFRunLoopStop(CFRunLoopGetCurrent());
// }
//
