# Generated by ProjectGen. Edit project.cfg, not this file.
.DEFAULT_GOAL := all
MAKEFILE_DIR := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))
PROJECT_ROOT := $(abspath $(MAKEFILE_DIR)/../..)
CXX ?= g++
AR ?= ar
CONFIGURATION ?= [DEFAULT_CONFIGURATION]
TARGETS :=
DEPFILES :=

[CONFIGURATIONS]ifeq ($(CONFIGURATION_SUPPORTED),)
$(error unsupported CONFIGURATION '$(CONFIGURATION)')
endif

.PHONY: all build debug release clean
all: build
build: $(TARGETS)
[BUILD_HELPERS]clean:
	rm -rf "$(MAKEFILE_DIR)/output" "$(MAKEFILE_DIR)/obj"

-include $(DEPFILES)
