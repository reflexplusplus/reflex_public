# Building
* Select the variant (open `Build Variants` from Android Studio in `View` -> `Tool Windows`), select `Release`.
* Build (use Ctrl+F9 / `Make Project`). You cannot actually "run" the target, like a normal Android application.
* The output will be: `bin/lib/android/outputs/aar/reflex-debug.aar`.
