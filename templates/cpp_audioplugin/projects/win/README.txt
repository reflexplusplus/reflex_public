Local Windows paths

Paths.props contains machine-specific paths used by the Visual Studio projects.
It is intentionally ignored by Git so different developers and machines can use
their own Reflex installation and output locations without shared-path conflicts.

Paths.props.RENAME is the checked-in starter file. On a new checkout, copy or
rename it to Paths.props, then adjust the local values as needed.
