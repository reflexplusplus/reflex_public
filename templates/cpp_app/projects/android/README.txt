Local Android paths

local.properties contains machine-specific paths used by the Android project.
It is intentionally ignored by Git so different developers and machines can use
their own SDK and Reflex installation paths without shared-path conflicts.

local.properties.RENAME is the checked-in starter file. On a new checkout, copy
or rename it to local.properties, then adjust the local values as needed.
