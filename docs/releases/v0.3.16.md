v0.3.16 — public SDK no longer ships the VM

The virtual machine is removed from the public SDK completely, including the binary mirror. CI pins the macOS toolchain, adds runner smoke tests, and publishes hyphenated tags as prereleases.

## Changes
### ci
- ci: pin macOS toolchain, add runner smoke tests
- ci: delete stale signing keychain before importing certs
- ci: publish hyphenated tags as prereleases
- ci(export): default the export tag to the selected ref
- ci: gate the release on consuming the assembled distribution
- ci: resolve cmake on macOS, assert in the smoke test, waive Linux verify

### cmake
- cmake: fix iOS library detection against Xcode-substituted paths
- cmake: correct the case of the UI library names

### export
- export: remove the VM from the public SDK completely
- export: strip VM libraries from the public binary mirror
- export: guard bootstrap/common.h's VM include for the public SDK

### ide
- ide: match Start's definition to its declaration

### other
- Removed ReflexCommonVmUI from the windows project
- Removed CommonVmUi library from android project
- VM enhancements phase #2
- Address pre-merge review of the iOS fix and the release gate

### tests
- tests: compile the dist probe under every bootstrap type

### win
- win: name the UI library consistently, and drop the dead VmUi target

Co-Authored-By: Adam Wilson <14980+adamski@users.noreply.github.com>
Co-Authored-By: Florian <9025750+Brunni132@users.noreply.github.com>
Co-Authored-By: jameswalkerhall <118645168+jameswalkerhall@users.noreply.github.com>
