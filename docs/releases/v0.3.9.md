v0.3.9 — ReflexCLI install and create fixes

Repairs ReflexCLI install after an API change, adds a prompted create flow, and corrects the help output. GLX now fails on invalid layer names instead of accepting them silently.

## Changes
### other
- export-repos fix pull url
- ReflexCLI install latest was broken after api change
- ReflexCLI correct help
- ReflexCLI create prompted
- GLX fix invalid layer names did not fail
- GLX::Detail::ToPoint helper
- fix GLX::TabGroup::Clear
- GLX widget header file consistency tweaks
- GLX Align layer normalized property + Circle layers fit property
- GLX layers offset indent-shorthand dynamic-binding support
- Reflex::Trim dedicated overloads to avoid ambiguity
- System::AudioPlugin::NoteInfo

### templates
- templates: add reflex_leaks.txt to gitignore

Co-Authored-By: jameswalkerhall <118645168+jameswalkerhall@users.noreply.github.com>
